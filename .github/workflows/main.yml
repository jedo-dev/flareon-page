name: 🚀 Deploy to local folder

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted 
    
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: 📂 Copy files to target directory
        run: |
          sudo rm -rf /mnt/HP_P500/flareon/*
          sudo cp -R ./ /mnt/HP_P500/flareon/
